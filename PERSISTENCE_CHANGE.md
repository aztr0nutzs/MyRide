# Persistence Change Prompt

Implement the persistence change without data loss.

Inspect current schema and migrations. Define target schema. Preserve stable IDs and relationships. Design a non-destructive migration. Add tests for empty and populated databases, edge cases, export/import, and invalid input. Validate before mutation. Never solve schema problems by deleting the database.
