# Migration Prompt

Inspect current and target schemas. Design a deterministic, non-destructive migration. Preserve stable IDs, relationships, timestamps, and user content. Test empty, populated, edge-case, and legacy data. Test export/import compatibility. Verify record counts and relationships before and after.
